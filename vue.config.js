//Configuración del proyecto.
module.exports = {
    // Modificar configuración web-pack
    //configureWebpack: config => {}
    //Añadir nuevos Loaders
    //,chainWebpack: config => { }
    //En ocasiones el path de producción no se encuentra en la raiz,a veces se
    //encuentra dentro de un SubSite, por lo que hay que indicar este Path URL/SubPath
    //,publicPath: process.env.NODE_ENV === 'production'? '/produccion/': '/'
}